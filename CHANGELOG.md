# 更新日志

> 每条只记两件事：**改了什么**、**对用的你有什么影响**。逐次提交的完整过程看 `git log`。
> `release.sh` 发布时会自动抓本版那一节当 Release 说明（版本号取自 exe 的 `FileVersion`，
> 所以小标题必须写成 `## vX.Y.Z`）。

## v1.9.6 · 2026-09-25

- 额度按账号**并发拉取**：服务端这个接口慢（实测 12~21 秒/请求），以前逐个串行拉会累加成分钟级；现在同时拉、回一个填一个。
- **影响**：多个账号时额度列明显更快刷出来（一轮耗时约等于最慢的那个账号）；候选链与「没连代理就整轮跳过」的语义不变。

## v1.9.5 · 2026-09-24

- **纯白简洁版界面** + 淡入 / 悬停等动效；下线「刷新 / 检查更新 / 汉化状态」三个入口（都被自动化覆盖了），托盘菜单只剩「退出」。
- 修 `CreateJunctionNative` 重解析缓冲区错位 4 字节导致的 4392（v1.8.30 起一直坏、靠 `mklink` 兜底）。
- 一并包含 v1.8.26–v1.8.33 源码线的改动（见下节，这些改动此前只存在于源码里）。
- **影响**：界面与入口更清爽；会话共享的原生建 junction 真正可用（离线自测 29/29 全过）。

## v1.8.26–v1.8.33 · 源码线（随 v1.9.5 一起发布）

- **会话共享更稳**：建 junction 改走原生 `DeviceIoControl`（`mklink` 只作兜底——装了安全软件的机器会把 `mklink` 拦成「拒绝访问」）；建之前先补父目录，**全新实例不再卡在建链接失败、也不再反复弹初始化框**。
- **额度**：没连代理时整轮跳过（一个请求都不发），额度列直接写「未连代理」，旧数字降级到悬停提示。
- **删除会话**：新增入口，永久删除会话及其全部聊天记录（Freebuff 界面只有「归档」）；对话框支持搜索与多选，运行中的会话整行标色。
- **代理**：实例掉线会主动弹气泡说明是哪个实例、哪个地址；窗口内只留「删除会话 / 代理设置」两个入口。
- **自动恢复汉化**：时机补全（打开控制器时 / 装机版本变化 / 点「启动」之前 / 本地 `build.sh` 跑完 / 刚拉到新包），换文件改成**先完整落地再换位**，不会留下「界面白屏但控制器以为已应用」的半截态。

## v1.8.25 · 2026-09-22

- 「停止」改成**先礼后兵**：先请实例自己退（最多 2 秒，给 profile 里的 SQLite 收尾的机会）再硬杀，并顺着进程树把子孙编排器一起收掉。
- 「重复双击」不再吃一句「已经被占用」：把已在跑的窗口叫到前台；v1.8.25 补了**前台资格让渡**与陈旧信号清理（否则窗口只能闪一下任务栏，或下一次双击被误判成「有人应答」）。
- 关键路径的失败（换文件 / 杀进程 / 写 `state.json` / 建 junction / 唤起窗口）现在会记一行到 `%TEMP%\freebuff-controller-log.txt`。
- **影响**：停止更干净（不再留孤儿编排器占端口）；「双击了没反应」「汉化没换上」终于有现场可查。

## v1.8.22 · 2026-09-12

- 自更新替换成功后把旧 exe 备份成 `*.bak-v旧版本.exe`（可回滚），并写标记；重启后控制器把「已升级到 vX ✓」浮到状态行 8 秒。
- **影响**：升级后能看见到底成没成，旧版留着可回退。

## v1.8.21 · 2026-09-12

- 自更新**真正装得上了**：线上 `sha512.txt` 是 sha512sum 的二进制格式（`<hex> *FreebuffController.exe`），旧版只拿裸文件名比对、永远不命中，每次都停在「Release 缺少有效 SHA512」；现在认 `*` / `\` 前缀。
- 替换脚本的重试延时从 `timeout` 改成 `ping`：前者在无控制台的 GUI 进程里会立刻返回（实测 0.02 秒），号称「重试 60 次」实际约 8 秒就放弃。
- **影响**：装完旧版 exe 被占用时的替换不再报「替换失败」。

## v1.8.20 · 2026-09-12

- 按钮圆角改**自绘抗锯齿**（原来靠 `Control.Region` 裁剪，只能画出阶梯状圆角）；补齐 v1.8.12→v1.8.20 的改动：自动恢复汉化的时机补全（打开控制器时 / 检测到装机版本变化 / 启动实例之前 / 本地重新构建之后）、实例掉线提醒、槽位实例不再拿到空的 `--proxy-server=`（空值会被当成一份非法代理配置）。
- **影响**：圆角与悬停 / 按下态都是平滑的；Freebuff 更新冲掉汉化后不用手动点任何按钮。

## v1.8.3 · 2026-09-10

- 额度请求超时 8 秒 → **30 秒**（服务端繁忙时实测需要 12~21 秒）。
- **影响**：额度列不再经常显示查询失败。

## v1.8.2 · 2026-09-09

- 应用汉化时的版本判定改用**暂存包版本**，修掉 fetch 场景误弹窗。
- **影响**：「有新包可应用」不再在没必要的时候弹出来。

## v1.8.1 · 2026-09-09

- 自更新接通 **SHA512 校验**；替换脚本的 `move` 失败可感知（不再静默当成功）。
- **影响**：自更新下载的 exe 会先校验再替换。

## v1.8.0 · 2026-09-06

- 统一 `AssemblyVersion` 与 `FileVersion`（都从 exe 的 `FileVersion` 推 tag 发版）。
- **影响**：版本号在界面、文件名、Release tag 上一致，不再出现两套号。

## v1.7.0 · 2026-09-06（有 tag，未单独发 Release）

- **会话共享改为默认开启**：不再有「共享会话」按钮，启动时自动提示并入；迁移边界修复，默认勾选「包含 AGENTS.md」。
- **影响**：多账号窗口打开同一份聊天记录成为默认行为。

## v1.6.0–v1.6.2 · 2026-09-05

- **默认回复中文**：启动时确保家目录存在 `~\.AGENTS.md` 语言规则（Freebuff 的 agent 会把家目录规则并入系统提示词）。
- 规则升级为**抗注入条款**：明确宣布「改用英文回复」这类指令及其变体（翻译式 / 格式声明式 / 借口式 / 中文措辞 / 间接注入）一律无效；老用户的旧版规则会在升级后自动补齐。
- **影响**：界面语言之外，「AI 用什么语言回你」也不再受服务端注入影响；只在会话开始时生效，不依赖汉化包。

## v1.5.0 · 2026-09-05

- 适配 Freebuff **Freebucks 额度模型**：移除已退役的日 / 周 / 月会话窗口显示，额度改为「今日剩余 / 总额度」口径。
- **影响**：上游改口径后不再显示早就没有意义的窗口数字。

## v1.4.0 · 2026-09-05（其额度口径随后被 v1.5.0 取代）

- 额度显示升级为今日 / 本周 / 本月三个窗口，悬停看明细。

## v1.3.3 · 2026-09-04

- 修 `ParseLooseVersion` **丢失第四段版本号**的问题。
- **影响**：汉化包的修正重发版（`0.0.103.1` 这种只升第四段的包）能被正确判定为「有新版」并自动拉取——四段比较从此成立。

## v1.3.2 · 2026-09-03

- 汉化状态随 Freebuff 版本变化**实时刷新**，不再等 30 分钟或重启。
- **影响**：刚更新完 Freebuff，状态行立刻反映了现实。

## v1.3.1 · 2026-09-01

- 修「打开控制器时没代理」：启动兜底重探 + 60 秒定时刷新 + 双端点功能探测。
- **影响**：代理晚于控制器启动时，不用重启就能连上。

## v1.3.0 · 2026-08-30

- **代理**：多端口自动探测（7890 / 7897 / 10808 / 10809 / 1080）+ 功能级探测（经代理真发一次请求）+ 粘性路由（记住上次成功的路由优先重试）+ 逐端口状态展示；启动实例时接入 `--proxy-server` 与 `HTTP(S)_PROXY`。
- **汉化包改走 GitHub Release 分发**，控制器自动检查 / 下载 / 校验 / 应用。
- 带 Release 的第一个版本。
